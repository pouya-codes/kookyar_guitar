<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryColorDark</item>
        <item name="colorAccent">@color/accentColor</item>
        <item name="materialButtonStyle">@style/Widget.App.Button</item>
        <item name="buttonStyle">@style/Widget.App.Button.Legacy</item>
    </style>

    <!-- Button style that preserves custom backgrounds -->
    <style name="Widget.App.Button" parent="Widget.Material3.Button">
        <item name="backgroundTint">@null</item>
    </style>
    <style name="Widget.App.Button.Legacy" parent="android:Widget.Button">
        <item name="android:backgroundTint">@null</item>
    </style>

    <style name="DisableSoundEffects" parent="AppTheme">
        <item name="android:soundEffectsEnabled">false</item>
    </style>

    <style name="customeToolbar" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
    </style>

</resources>
